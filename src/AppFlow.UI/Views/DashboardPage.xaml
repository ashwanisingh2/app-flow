<Page
    x:Class="AppFlow.UI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppFlow.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold"/>

        <!-- Stat Cards -->
        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="{StaticResource AppFlowCardCornerRadius}" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    Padding="{StaticResource AppFlowCardPadding}">
                <StackPanel>
                    <FontIcon Glyph="&#xE718;" HorizontalAlignment="Left" Foreground="{StaticResource AppFlowAccentBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.TotalInstalled, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="0,8,0,0"/>
                    <TextBlock Text="Total Installed" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" CornerRadius="{StaticResource AppFlowCardCornerRadius}" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    Padding="{StaticResource AppFlowCardPadding}">
                <StackPanel>
                    <FontIcon Glyph="&#xE777;" HorizontalAlignment="Left" Foreground="{StaticResource AppFlowSuccessBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.UpdatesAvailable, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="0,8,0,0"/>
                    <TextBlock Text="Updates Available" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" CornerRadius="{StaticResource AppFlowCardCornerRadius}" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    Padding="{StaticResource AppFlowCardPadding}">
                <StackPanel>
                    <FontIcon Glyph="&#xE81C;" HorizontalAlignment="Left" Foreground="{StaticResource AppFlowWarningBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.RecentActionsCount, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="0,8,0,0"/>
                    <TextBlock Text="Recent Actions" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Recent Activity -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Recent Activity" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold" Margin="0,0,0,12"/>
            
            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.RecentActions, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="12" Padding="0,8">
                            <FontIcon Glyph="&#xE73E;" Foreground="{StaticResource AppFlowSuccessBrush}"
                                      Visibility="{Binding Success, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <FontIcon Glyph="&#xE711;" Foreground="{StaticResource AppFlowDangerBrush}" 
                                      Visibility="{Binding Success, Converter={StaticResource BoolNegationConverter}}"/>
                            <TextBlock Text="{Binding PackageName}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ActionType}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Timestamp}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>

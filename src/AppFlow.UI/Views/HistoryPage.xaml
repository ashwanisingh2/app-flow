<Page
    x:Class="AppFlow.UI.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppFlow.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="16">
            <TextBlock Text="Action History" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBox PlaceholderText="Search history..." Width="200" VerticalAlignment="Center"/>
            <ComboBox PlaceholderText="Filter action" VerticalAlignment="Center" Width="150">
                <x:String>All</x:String>
                <x:String>Install</x:String>
                <x:String>Update</x:String>
                <x:String>Uninstall</x:String>
            </ComboBox>
        </StackPanel>

        <ListView Grid.Row="1">
            <!-- Binding omitted for simplicity of MVP, logic goes to viewmodel -->
        </ListView>
    </Grid>
</Page>

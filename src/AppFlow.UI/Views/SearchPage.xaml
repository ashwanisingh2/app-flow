<Page
    x:Class="AppFlow.UI.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppFlow.UI.Views"
    xmlns:controls="using:AppFlow.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Search Packages" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <AutoSuggestBox Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}"
                            PlaceholderText="Search (e.g. firefox, vscode)..."
                            QueryIcon="Find"
                            Width="400"
                            QuerySubmitted="AutoSuggestBox_QuerySubmitted"/>
                            
            <ComboBox SelectedItem="{x:Bind ViewModel.SelectedFilter, Mode=TwoWay}" Width="150">
                <x:String>All</x:String>
                <x:String>Installed</x:String>
                <x:String>Updates</x:String>
            </ComboBox>
        </StackPanel>

        <Grid Grid.Row="2">
            <ProgressRing IsActive="{x:Bind ViewModel.IsSearching, Mode=OneWay}" 
                          HorizontalAlignment="Center" VerticalAlignment="Center" 
                          Width="48" Height="48"/>
                          
            <GridView ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}"
                      ItemClick="GridView_ItemClick"
                      IsItemClickEnabled="True"
                      SelectionMode="None"
                      Padding="0,0,0,24">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <!-- We'll use a standard Border for now, can be refactored to AppCard Control -->
                        <Border Width="280" Height="120" 
                                CornerRadius="{StaticResource AppFlowCardCornerRadius}"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                                Padding="{StaticResource AppFlowCardPadding}"
                                Margin="0,0,16,16">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Publisher}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                           FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,4,0,8"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                                            CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{Binding SourceId}" FontSize="11"/>
                                    </Border>
                                    <TextBlock Text="{Binding LatestVersion}" FontSize="11" 
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>
    </Grid>
</Page>
